# CoffeeRatingApplication
Coffee Rating Application is a Flask-based web application that allows users to vote for different coffee types. Each vote updates the count in a SQLite database and displays the updated rating on the webpage. This project demonstrates database updates, backend logic handling, and dynamic content rendering using Flask and SQLAlchemy.
